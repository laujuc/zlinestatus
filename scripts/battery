#!/bin/sh

set -eu

wait_seconds=5
if [ -r cfg/WAIT ]; then
    wait_seconds=$(head -n 1 cfg/WAIT)
fi

find_battery_dir() {
    for dir in /sys/class/power_supply/BAT*; do
        if [ -r "$dir/capacity" ]; then
            printf '%s\n' "$dir"
            return 0
        fi
    done
    return 1
}

while :; do
    battery_dir=$(find_battery_dir || true)

    if [ -n "${battery_dir}" ] && [ -r "$battery_dir/status" ]; then
        capacity=$(head -n 1 "$battery_dir/capacity")
        status=$(head -n 1 "$battery_dir/status" | tr '[:upper:] ' '[:lower:]-')

        case "$status" in
            charging)
                zsendvalue -type battery "$capacity" charging battery ac
                ;;
            full)
                zsendvalue -type battery "$capacity" full battery ac
                ;;
            discharging)
                zsendvalue -type battery "$capacity" discharging battery
                ;;
            *)
                zsendvalue -type battery "$capacity" "$status" battery
                ;;
        esac
    else
        zsendvalue -type battery 0 ac
    fi

    sleep "$wait_seconds"
done
